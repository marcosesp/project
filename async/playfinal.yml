---
- name: Actualizar Sistema con Logs en Tiempo Real
  hosts: all
  gather_facts: false

  tasks:
    - name: Ejecutar actualización de DNF con streaming directo
      ansible.builtin.shell: |
        stdbuf -oL -eL dnf -y update
